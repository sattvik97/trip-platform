# Backend Environment Configuration - TEST
# Copy this file to .env for test environment
# DO NOT commit .env files with real secrets
#
# âš ï¸  WARNING: TEST mode uses Azure PostgreSQL database (same as prod)
# This is intended for pre-production testing against production database
# Use with caution - any changes will affect production data
#
# To enable test mode, set ENV=test in your environment or .env file

# ============================================================================
# ENVIRONMENT
# ============================================================================
# Must be set to "test" to enable test mode
ENV=test

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Azure PostgreSQL connection string
# Format: postgresql://username:password@server.postgres.database.azure.com:5432/dbname?sslmode=require
# 
# âš ï¸  WARNING: This points to the SAME database as production
# All database changes in test mode will affect production data
DATABASE_URL=postgresql://username:password@your-server.postgres.database.azure.com:5432/tripdb?sslmode=require

# ============================================================================
# JWT CONFIGURATION
# ============================================================================
# Secret key for JWT token signing
# Should match production secret key for consistent token validation
SECRET_KEY=your-secret-key-change-in-production

# JWT algorithm (HS256 recommended)
ALGORITHM=HS256

# Token expiration time in hours
ACCESS_TOKEN_EXPIRE_HOURS=24

# ============================================================================
# AZURE BLOB STORAGE CONFIGURATION
# ============================================================================
# REQUIRED for test environment
# Azure Storage Account connection string
# Format: DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net
BLOB_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-storage-account;AccountKey=your-account-key;EndpointSuffix=core.windows.net

# Blob container name for storing images
# Container will be created automatically if it doesn't exist
BLOB_CONTAINER=trip-images

# ============================================================================
# LOCAL STORAGE CONFIGURATION
# ============================================================================
# IGNORED in test environment (uses Azure Blob Storage)
# Leave as-is or remove
LOCAL_UPLOAD_DIR=media

# ============================================================================
# CORS CONFIGURATION
# ============================================================================
# Comma-separated list of allowed frontend origins
# For test environment, include your local frontend origin
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
