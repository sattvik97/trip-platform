# Backend Environment Configuration - PRODUCTION
# 
# âš ï¸  IMPORTANT: This file is for REFERENCE ONLY
# 
# In production (ENV=prod), .env files are NOT loaded.
# All configuration must come from environment variables set in Azure App Service.
#
# DO NOT create a .env file in production - use Azure App Service Configuration instead.
#
# To configure production environment:
# 1. Go to Azure Portal â†’ Your App Service â†’ Configuration
# 2. Add each of these variables as Application Settings
# 3. Never commit real secrets to version control

# ============================================================================
# ENVIRONMENT
# ============================================================================
# Must be set to "prod" in production
ENV=prod

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Azure PostgreSQL connection string
# Format: postgresql://username:password@server.postgres.database.azure.com:5432/dbname?sslmode=require
# Set in Azure App Service as: DATABASE_URL
DATABASE_URL=postgresql://username:password@your-server.postgres.database.azure.com:5432/tripdb?sslmode=require

# ============================================================================
# JWT CONFIGURATION
# ============================================================================
# Secret key for JWT token signing
# IMPORTANT: Use a strong, randomly generated secret key
# Set in Azure App Service as: SECRET_KEY
SECRET_KEY=your-secret-key-change-in-production

# JWT algorithm (HS256 recommended)
# Set in Azure App Service as: ALGORITHM
ALGORITHM=HS256

# Token expiration time in hours
# Set in Azure App Service as: ACCESS_TOKEN_EXPIRE_HOURS
ACCESS_TOKEN_EXPIRE_HOURS=24

# ============================================================================
# AZURE BLOB STORAGE CONFIGURATION
# ============================================================================
# REQUIRED for production
# Azure Storage Account connection string
# Format: DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net
# Set in Azure App Service as: BLOB_CONNECTION_STRING
BLOB_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-storage-account;AccountKey=your-account-key;EndpointSuffix=core.windows.net

# Blob container name for storing images
# Set in Azure App Service as: BLOB_CONTAINER
BLOB_CONTAINER=trip-images

# ============================================================================
# LOCAL STORAGE CONFIGURATION
# ============================================================================
# NOT USED in production (uses Azure Blob Storage)
# Can be omitted or left empty
LOCAL_UPLOAD_DIR=media

# ============================================================================
# CORS CONFIGURATION
# ============================================================================
# Comma-separated list of allowed frontend origins
# Should include your production frontend URL (Azure Static Web Apps)
# Set in Azure App Service as: CORS_ORIGINS
CORS_ORIGINS=https://your-frontend-app.azurestaticapps.net,https://your-custom-domain.com
